
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Running Expense Manager Build"
                sh "ls"
                echo "Build Successful"
            }
        }
    }
}
