pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh '''
                echo "Running Expense Manager Build"
                ls
                echo "Build Successful"
                '''
            }
        }
    }
}
